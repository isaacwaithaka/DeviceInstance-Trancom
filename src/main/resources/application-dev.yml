# ===============================
# = Server Cofiguration
# ===============================
server:
  port: 8072
  servlet:
    context-path: /

spring:
  jpa:
    database: mysql
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

  datasource:
#    platform: mysql
    url: jdbc:mysql://localhost:3306/trancom?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: root
    password: NkG3NLhFgK/E03LOlHw=
